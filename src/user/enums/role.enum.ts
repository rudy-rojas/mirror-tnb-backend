// role.enum.ts
export enum Role {
  ADMIN = 'admin',
  MANAGER = 'manager',
  AGENT = 'agent',
  PROVIDER = 'provider',
  CLIENT = 'client', // lead
  SUPPORT = 'support',
  MARKETING = 'marketing',
  FINANCE = 'finance',
}
